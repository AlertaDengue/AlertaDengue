version: "2"
services:
    memcache:
        image: memcached:latest
        hostname: memcached
        ports:
            - 11212:11211
        restart: always

    web:
        build: .
        ports:
            - 8000:8000
        env_file:
            - .env
        environment:
            - MEMCACHED_HOST=memcache
        volumes:
            - ${DOCKER_HOST_MEDIA_ROOT}:/MEDIA_ROOT
        restart: always
